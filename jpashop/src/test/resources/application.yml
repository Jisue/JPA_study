# 스프링 부트는 기본적으로 메모리 모드로 돌리기 때문에 사실 아래 주석들은 없어도 됨
spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 모드로 테스트
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        # show_sql: true
#        format_sql: true
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace